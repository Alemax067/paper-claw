{
  "name": "paper-claw",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "concurrently -n root,frontend,backend \"npm install\" \"npm install --prefix frontend\" \"uv sync --project backend\"",
    "dev": "concurrently -n frontend,backend \"npm run dev --prefix frontend\" \"uv run --project backend backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
